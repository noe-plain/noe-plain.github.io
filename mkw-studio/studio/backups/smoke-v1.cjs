const {chromium}=require(process.env.PLAYWRIGHT_PATH||'playwright');
(async()=>{const browser=await chromium.launch({headless:true,executablePath:'/Applications/Google Chrome.app/Contents/MacOS/Google Chrome'});const page=await browser.newPage({viewport:{width:1512,height:982}});const urls=[];page.on('request',r=>urls.push(r.url()));await page.goto('http://127.0.0.1:8765');await page.waitForFunction(()=>fontStatus['Replica LL']);if(await page.locator('#empty').isHidden())throw Error('Kein leeres Startprojekt');if(await page.locator('#files').isVisible())throw Error('Interner Dateieingang sichtbar');if(urls.some(u=>!u.startsWith('http://127.0.0.1:8765/')&&!u.startsWith('data:')))throw Error('Externe Anfrage');await page.screenshot({path:'docs/Startansicht.png'});console.log('PASS fertiger Startmechanismus, leeres Projekt, Replica geladen, keine externen Anfragen');await browser.close()})().catch(e=>{console.error(e);process.exit(1)});
